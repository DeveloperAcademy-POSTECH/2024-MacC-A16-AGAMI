analyzer_rules:
  - unused_declaration

disabled_rules:
  - line_length
  - trailing_whitespace
  - trailing_newline

opt_in_rules:
  - empty_count
  - first_where

included:
  - Solog/Sources

excluded:
  - Tests
  - Carthage
  - Pods
